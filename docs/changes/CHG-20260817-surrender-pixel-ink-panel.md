# CHG-20260817：劝降像素水墨军情牒文

- 状态：`done`
- 日期：2026-08-17

## 目标

将规整米色劝降卡片替换为符合项目像素水墨风的破边军情牒文，同时保留既有劝降规则、按钮行为和教程浮层避让。

## 范围

- 使用内置 `imagegen` 将暖黄旧宣纸迭代为透明冷青灰布帛、海浪形深蓝墨晕与克制朱砂细节的无文字底板。
- 面板调整为 `520×200px`，增加上下留白；按钮缩至 `88–94×34px`，避免标题、正文与操作区拥挤。
- 劝降/接受按钮使用暗朱砂，拒绝按钮使用深墨青；所有关卡由 HUD 运行时统一补建并加载底板。
- 不修改投降规则、成功率、交付逻辑与战斗状态。

## 验收

- 不再显示程序米色矩形卡片。
- 生成底板透明边缘完整，文字与按钮位于纸面安全区。
- 单按钮和双按钮布局均居中，无隐藏控件占位。
- 编译、专项测试和 OpenGL 实机截图通过。

## 生成与验证证据

- 使用内置 `imagegen` 生成透明无文字底板；原始输出保留于 Codex 生成目录。
- 项目素材为 `assets/naval/ui/surrender/surrender_dispatch_panel_v2.png`，尺寸 `1200×480` RGBA，透明角像素为 `(0,0,0,0)`；`v1` 原稿保留用于回溯。
- Godot 4.7.1 导入成功；实机显示尺寸 `520×200px`。
- `dotnet build --no-restore`：通过，0 警告、0 错误。
- `test_naval_tutorial_hint.gd`：通过，覆盖生成底板、尺寸、标题层级与教程避让。
- 1344×896 OpenGL 单按钮、双按钮截图：通过，文字与按钮无裁切，透明破边正常。
