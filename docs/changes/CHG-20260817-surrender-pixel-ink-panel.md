# CHG-20260817：劝降像素水墨笔触提示

- 状态：`done`
- 日期：2026-08-17

## 目标

将劝降卡片统一为项目既有的像素水墨干笔墨条，同时保留既有劝降规则、按钮行为和教程浮层避让。

## 范围

- 最终复用 `tutorial_dry_brush_strip_v1.png`，移除两版独立宣纸/布帛素材，避免与海战背景和教程风格割裂。
- 面板调整为 `600×150px` 横向笔触；正文使用米白黑描边，标题保持金色黑描边。
- 操作按钮常态取消矩形底框，只保留 `76–92×30px` 点击热区和文字；所有关卡由 HUD 运行时统一加载墨条。
- 不修改投降规则、成功率、交付逻辑与战斗状态。

## 验收

- 不再显示程序米色矩形卡片。
- 复用墨条透明边缘完整，文字与操作热区位于笔触安全区。
- 单按钮和双按钮布局均居中，无隐藏控件占位。
- 编译、专项测试和 OpenGL 实机截图通过。

## 素材与验证证据

- 前两版独立生图底板已从项目删除；最终界面不新增图片素材。
- 劝降界面复用项目素材 `assets/naval/ui/tutorial/tutorial_dry_brush_strip_v1.png`，不再保留独立劝降底板。
- Godot 4.7.1 导入成功；实机显示尺寸 `600×150px`。
- `dotnet build --no-restore`：通过，0 警告、0 错误。
- `test_naval_tutorial_hint.gd`：通过，覆盖墨条底板、尺寸、标题层级与教程避让。
- 1344×896 OpenGL 单按钮、双按钮截图：通过，文字与按钮无裁切，透明破边正常。
